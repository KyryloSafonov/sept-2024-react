import { FC } from 'react';

import { RoutesComponent } from 'routes/Routes';

import 'App.css';

const App: FC = () => {
    return (
        <div>
            <RoutesComponent />
        </div>
    );
};

export default App;
