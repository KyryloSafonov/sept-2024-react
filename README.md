# React + TypeScript + Vite

- For install dependencies use `yarn install` in terminal
- For starts dev server use `yarn dev` in terminal
- For build a production package use `yarn build` in terminal

This project use a Vite as HMR and React with TS as a front-end main library

### If some problem with github pages deploy check this link
https://github.com/ErickKS/vite-react-router
